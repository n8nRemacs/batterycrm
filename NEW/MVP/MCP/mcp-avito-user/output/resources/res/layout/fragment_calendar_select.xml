<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:id="@+id/calendar_screen_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <include layout="@layout/calendar_select_view_header"/>
    <include layout="@layout/calendar_select_week_days"/>
    <FrameLayout
        android:id="@+id/content_holder"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:paddingLeft="@dimen/calendar_select_recycler_view_padding"
            android:paddingRight="@dimen/calendar_select_recycler_view_padding"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
        <LinearLayout
            android:layout_gravity="bottom"
            android:orientation="horizontal"
            android:id="@+id/calendar_buttons_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="26dp"
            android:layout_marginVertical="16dp">
            <com.avito.android.lib.design.button.Button
                android:id="@+id/button_select"
                android:visibility="gone"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@string/calendar_select_apply"
                android:layout_weight="1"
                style="?attr/buttonPrimaryLarge"/>
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
