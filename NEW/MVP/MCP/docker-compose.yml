services:
  input-contour:
    build: ./input-contour
    container_name: input-contour
    restart: unless-stopped
    environment:
      - REDIS_URL=redis://:Mi31415926pSss!@redis:6379/0
      - CLIENT_CONTOUR_URL=http://client-contour:8772/resolve
      - DEBOUNCE_SECONDS=10
      - MAX_WAIT_SECONDS=300
      - POLL_INTERVAL=1.0
      - BATCH_SIZE=100
    ports:
      - "8771:8771"
    networks:
      - mcp_net
      - redis_app_net
    depends_on:
      - client-contour

  client-contour:
    build: ./client-contour
    container_name: client-contour
    restart: unless-stopped
    environment:
      - CORE_URL=https://n8n.n8nsrv.ru/webhook/elo-core-ingest
      - REDIS_URL=redis://:Mi31415926pSss!@redis:6379/0
      - HOST=0.0.0.0
      - PORT=8772
      - DATABASE_URL=postgresql://app_user:Mi31415926pS@185.221.214.83:6544/postgres
      - NEO4J_URI=bolt+ssc://155.212.221.189:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=Mi31415926pS
    ports:
      - "8772:8772"
    networks:
      - mcp_net
      - redis_app_net

  graph-tool:
    build: ./graph-tool
    container_name: graph-tool
    restart: unless-stopped
    image: mcp-graph-tool
    environment:
      - DATABASE_URL=postgresql://app_user:Mi31415926pS@185.221.214.83:6544/postgres
      - NEO4J_URI=bolt+ssc://155.212.221.189:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=Mi31415926pS
      - HOST=0.0.0.0
      - PORT=8773
    ports:
      - "8773:8773"
    networks:
      - mcp_net
      - redis_app_net

networks:
  mcp_net:
    driver: bridge
  redis_app_net:
    external: true
