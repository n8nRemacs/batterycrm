# ТЗ: Веб-приложение оператора Eldoleado

**Версия:** 1.0
**Дата:** 2025-12-29
**Основа:** Android приложение Eldoleado

---

## 1. Общее описание

Веб-приложение для операторов сервиса Eldoleado, запускаемое в браузере. Функционал аналогичен Android приложению: работа с диалогами клиентов через различные мессенджеры (WhatsApp, Telegram, Avito, VK, MAX).

### 1.1 Целевая аудитория
- Операторы сервисного центра
- Администраторы (настройка каналов)

### 1.2 Поддерживаемые браузеры
- Chrome 90+ (основной)
- Firefox 88+
- Safari 14+
- Edge 90+
- Opera 76+
- Яндекс Браузер 21+

### 1.3 Адаптивность
- Desktop: 1920x1080 Full HD (основной)
- Laptop: 1280px - 1919px
- Tablet: 768px - 1279px
- Mobile: 320px - 767px (упрощённый интерфейс)

---

## 2. Технологический стек

### 2.1 Frontend
| Технология | Назначение |
|------------|------------|
| **React 18+** | UI фреймворк |
| **TypeScript** | Типизация |
| **Vite** | Сборщик |
| **TailwindCSS** | Стили |
| **React Query** | Кеширование API запросов |
| **Zustand** | Глобальное состояние |
| **React Router v6** | Маршрутизация |
| **Socket.io-client** | WebSocket для real-time |

### 2.2 Дополнительные библиотеки
| Библиотека | Назначение |
|------------|------------|
| **react-hook-form** | Формы |
| **zod** | Валидация |
| **date-fns** | Работа с датами |
| **lucide-react** | Иконки |
| **react-hot-toast** | Уведомления |
| **qrcode.react** | QR-коды для WhatsApp |

### 2.3 API
- **Base URL:** `https://n8n.n8nsrv.ru/webhook/`
- **Формат:** REST JSON
- **Авторизация:** Bearer token в заголовке `Authorization`

---

## 3. Структура экранов

### 3.1 Карта навигации

```
/login                    → Авторизация
/                         → Редирект на /dialogs
/dialogs                  → Список диалогов (главный экран)
/dialogs/:id              → Чат с клиентом
/settings                 → Настройки
/settings/channels        → Управление каналами
/settings/channels/whatsapp  → Настройка WhatsApp
/settings/channels/telegram  → Настройка Telegram
/settings/channels/avito     → Настройка Avito
/settings/channels/max       → Настройка MAX
/settings/notifications   → Настройка уведомлений
```

### 3.2 Макет приложения

```
┌─────────────────────────────────────────────────────────┐
│  HEADER (logo, user info, logout)                       │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  SIDEBAR   │           MAIN CONTENT                     │
│  (меню)    │                                            │
│            │                                            │
│  - Диалоги │                                            │
│  - Настр.  │                                            │
│            │                                            │
└────────────┴────────────────────────────────────────────┘
```

---

## 4. Экраны приложения

### 4.1 Экран авторизации (`/login`)

**URL:** `/login`

**Макет:**
```
┌─────────────────────────────────────┐
│                                     │
│           [LOGO]                    │
│                                     │
│   ┌───────────────────────────┐     │
│   │ Email                     │     │
│   └───────────────────────────┘     │
│   ┌───────────────────────────┐     │
│   │ Пароль                    │     │
│   └───────────────────────────┘     │
│                                     │
│   [        ВОЙТИ        ]           │
│                                     │
│   Версия: 1.0.0                     │
└─────────────────────────────────────┘
```

**Функционал:**
- Поля: email, password
- Валидация: обязательные поля, формат email
- При успехе: сохранение session_token, редирект на `/dialogs`
- При ошибке: показ сообщения об ошибке
- Автоматическое определение device_info (browser, OS, version)

**API:**
```
POST /v1/auth/login
Body: {
  login: string,
  password: string,
  device_info: {
    device_id: string,      // fingerprint или UUID
    device_model: string,   // "Chrome 120 / Windows"
    os_version: string,     // "Windows 10"
    app_version: string     // "1.0.0-web"
  },
  app_mode: "client"
}

Response: {
  success: boolean,
  operator_id: string,
  tenant_id: string,
  name: string,
  session_token: string,
  allowed_channels: string[],
  channel_statuses: { [channel]: status }
}
```

---

### 4.2 Список диалогов (`/dialogs`)

**URL:** `/dialogs`

**Макет:**
```
┌─────────────────────────────────────────────────────────┐
│ HEADER: Диалоги                    [Поиск] [Фильтр ▼]   │
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [WA] Иван Петров                      14:32  (3)   │ │
│ │      Здравствуйте, сколько стоит...               │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [TG] Мария Сидорова                   13:15        │ │
│ │      Спасибо за ремонт!                           │ │
│ ├─────────────────────────────────────────────────────┤ │
│ │ [AV] Алексей                          вчера        │ │
│ │      🎤 Голосовое сообщение                        │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

**Элементы списка:**
| Элемент | Описание |
|---------|----------|
| Бейдж канала | Круглый, цветной (WA=зелёный, TG=синий, AV=оранжевый, VK=синий, MX=фиолетовый) |
| Имя клиента | Жирный если есть непрочитанные |
| Превью сообщения | Обрезается до 50 символов, иконка 🎤 для голосовых |
| Время | HH:mm для сегодня, "вчера", дата для старых |
| Счётчик непрочитанных | Красный бейдж справа |

**Сортировка:**
1. Непрочитанные сверху (по времени, новые выше)
2. Прочитанные ниже (по времени, новые выше)

**Фильтры:**
- Все каналы / WhatsApp / Telegram / Avito / VK / MAX
- Все / Непрочитанные

**API:**
```
GET /v1/dialogs?session_token={token}

Response: {
  success: boolean,
  dialogs: [{
    id: string,
    client_name: string,
    client_phone: string,
    channel: string,
    chat_id: string,
    last_message_text: string,
    last_message_time: number,    // timestamp
    last_message_is_voice: boolean,
    unread_count: number
  }]
}
```

**Real-time обновления:**
- WebSocket подписка на новые сообщения
- Обновление счётчика непрочитанных
- Перемещение диалога наверх при новом сообщении

---

### 4.3 Экран чата (`/dialogs/:id`)

**URL:** `/dialogs/:dialogId`

**Макет (Desktop):**
```
┌─────────────────────────────────────────────────────────┐
│ HEADER                                                  │
│ ← Назад   [WA] Иван Петров  +7 999 123-45-67   [📞]    │
├─────────────────────────────────────────────────────────┤
│ КАНАЛЫ ОТВЕТА                                           │
│ [TG] [WA●] [AV] [MX]                                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ СООБЩЕНИЯ                                               │
│                                                         │
│ ┌──────────────────────┐                                │
│ │ Клиент, 14:30        │                                │
│ │ Здравствуйте!        │                                │
│ └──────────────────────┘                                │
│                                                         │
│                        ┌──────────────────────┐         │
│                        │ Оператор, 14:32      │         │
│                        │ Добрый день!         │         │
│                        └──────────────────────┘         │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ ВВОД СООБЩЕНИЯ                                          │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Введите сообщение...                                │ │
│ └─────────────────────────────────────────────────────┘ │
│ [🎤 Голос] [✨ Нормализовать] [📤 Отправить]            │
└─────────────────────────────────────────────────────────┘
```

**Функционал:**

#### Заголовок
- Кнопка назад → `/dialogs`
- Бейдж канала (цветной)
- Имя клиента
- Телефон (кликабельный, `tel:`)
- Кнопка звонка (открывает `tel:`)

#### Выбор канала ответа (Omnichannel)
- Ряд кнопок с иконками каналов
- Активный канал подсвечен
- Позволяет ответить клиенту в другой канал

#### Список сообщений
- Входящие (клиент): слева, синий фон
- Исходящие (оператор): справа, оранжевый фон
- Формат времени: HH:mm
- Автопрокрутка к последнему сообщению
- Индикатор голосового сообщения (🎤)

#### Панель ввода
- Textarea (многострочный, расширяется)
- Кнопка очистки (×)
- Горячие клавиши: Ctrl+Enter = отправить

#### Кнопки действий
| Кнопка | Действие |
|--------|----------|
| 🎤 Голос | Голосовой ввод (Web Speech API) |
| ✨ Нормализовать | AI коррекция текста |
| 📤 Отправить | Отправка сообщения |

**API:**
```
GET /v1/messages?dialog_id={id}&session_token={token}

Response: {
  success: boolean,
  dialog: {
    id: string,
    client_name: string,
    client_phone: string,
    channel: string
  },
  messages: [{
    id: string,
    text: string,
    direction: "in" | "out",
    sender_type: string,
    sender_name: string,
    timestamp: number
  }]
}

POST /v1/messages/send
Body: {
  session_token: string,
  dialog_id: string,
  text: string,
  response_channel?: string  // для omnichannel
}

POST /v1/normalize
Body: {
  session_token: string,
  dialog_id: string,
  text: string
}
Response: {
  success: boolean,
  normalized_text: string
}
```

---

### 4.4 Настройки (`/settings`)

**URL:** `/settings`

**Макет:**
```
┌─────────────────────────────────────────────────────────┐
│ HEADER: Настройки                                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ КАНАЛЫ                                                  │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [WA] WhatsApp          Подключен ●     [Настроить] │ │
│ │ [TG] Telegram          Не настроен     [Настроить] │ │
│ │ [AV] Avito             Подключен ●     [Настроить] │ │
│ │ [VK] VKontakte         В разработке               │ │
│ │ [MX] MAX               Не настроен     [Настроить] │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ УВЕДОМЛЕНИЯ                                             │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Browser Notifications    [Вкл/Выкл]                │ │
│ │ Telegram Alerts          [Настроить]               │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ АККАУНТ                                                 │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Оператор: Иван Петров                              │ │
│ │ Email: ivan@example.com                            │ │
│ │ [Выйти]                                            │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 4.5 Настройка WhatsApp (`/settings/channels/whatsapp`)

**URL:** `/settings/channels/whatsapp`

**Этапы:**

#### Этап 1: Инициализация
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Подключение WhatsApp                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   Для подключения WhatsApp отсканируйте                │
│   QR-код в мобильном приложении WhatsApp               │
│                                                         │
│   [Начать подключение]                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### Этап 2: QR-код
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Подключение WhatsApp                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   Отсканируйте QR-код:                                 │
│                                                         │
│   ┌───────────────────┐                                │
│   │                   │                                │
│   │   [QR CODE]       │                                │
│   │                   │                                │
│   └───────────────────┘                                │
│                                                         │
│   Ожидание подключения...                              │
│   ⏱ Осталось: 1:45                                     │
│                                                         │
│   [Обновить QR]                                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### Этап 3: Успех
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Подключение WhatsApp                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ✅ WhatsApp подключен!                               │
│                                                         │
│   Номер: +7 999 123-45-67                              │
│   Имя: Иван Петров                                     │
│                                                         │
│   [Готово]                                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Логика:**
1. POST `/v1/channels/register` → получить `session_id`
2. Polling GET `http://155.212.221.189:8769/session/{id}/qr` каждые 3 сек
3. Отображение QR-кода (base64 или генерация через qrcode.react)
4. Polling GET `http://155.212.221.189:8769/session/{id}/status`
5. При status=connected → показать успех

**API (MCP WhatsApp Server):**
```
POST http://155.212.221.189:8769/session
Body: {
  tenantId: string,
  webhookUrl: string
}
Response: {
  sessionId: string,
  hash: string
}

GET http://155.212.221.189:8769/session/{id}/qr
Response: {
  qr: string  // base64 image или QR string
}

GET http://155.212.221.189:8769/session/{id}/status
Response: {
  status: "pending" | "connected" | "disconnected",
  phone?: string,
  name?: string
}
```

---

### 4.6 Настройка Telegram (`/settings/channels/telegram`)

**Этапы:**

#### Этап 1: Выбор типа
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Подключение Telegram                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   Выберите тип подключения:                            │
│                                                         │
│   ┌─────────────────────────────────────────────────┐   │
│   │ [🤖] Telegram Bot                               │   │
│   │     Для автоматических ответов через бота      │   │
│   └─────────────────────────────────────────────────┘   │
│                                                         │
│   ┌─────────────────────────────────────────────────┐   │
│   │ [👤] Telegram User                              │   │
│   │     Для работы от имени личного аккаунта       │   │
│   └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### Этап 2a: Bot Token
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Telegram Bot                                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   1. Откройте @BotFather в Telegram                    │
│   2. Создайте нового бота командой /newbot             │
│   3. Скопируйте токен и вставьте ниже                  │
│                                                         │
│   ┌─────────────────────────────────────────────────┐   │
│   │ Bot Token                                       │   │
│   └─────────────────────────────────────────────────┘   │
│                                                         │
│   [Проверить и подключить]                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### Этап 2b: User API
```
┌─────────────────────────────────────────────────────────┐
│ ← Назад    Telegram User                                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   1. Получите API ID и Hash на my.telegram.org         │
│                                                         │
│   ┌─────────────────────────────────────────────────┐   │
│   │ API ID                                          │   │
│   └─────────────────────────────────────────────────┘   │
│   ┌─────────────────────────────────────────────────┐   │
│   │ API Hash                                        │   │
│   └─────────────────────────────────────────────────┘   │
│   ┌─────────────────────────────────────────────────┐   │
│   │ Номер телефона                                  │   │
│   └─────────────────────────────────────────────────┘   │
│                                                         │
│   [Запросить код]                                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 5. Компоненты UI

### 5.1 Общие компоненты

| Компонент | Описание |
|-----------|----------|
| `Button` | Кнопка (primary, secondary, danger, ghost) |
| `Input` | Текстовое поле |
| `Textarea` | Многострочное поле |
| `Select` | Выпадающий список |
| `Switch` | Переключатель вкл/выкл |
| `Badge` | Бейдж (цветной, с текстом) |
| `ChannelBadge` | Бейдж канала (WA, TG, AV, VK, MX) |
| `Avatar` | Аватар пользователя |
| `Spinner` | Индикатор загрузки |
| `Toast` | Уведомление |
| `Modal` | Модальное окно |
| `BottomSheet` | Выезжающая панель снизу (mobile) |

### 5.2 Специфичные компоненты

| Компонент | Описание |
|-----------|----------|
| `DialogItem` | Элемент списка диалогов |
| `MessageBubble` | Пузырь сообщения |
| `MessageInput` | Поле ввода с кнопками |
| `ChannelSelector` | Выбор канала ответа |
| `QRCodeDisplay` | Отображение QR-кода |
| `VoiceRecorder` | Запись голоса |
| `ConnectionStatus` | Статус подключения |

### 5.3 Цветовая схема

```css
/* Основные цвета */
--primary: #2563eb;      /* Синий */
--secondary: #64748b;    /* Серый */
--success: #22c55e;      /* Зелёный */
--warning: #f59e0b;      /* Оранжевый */
--danger: #ef4444;       /* Красный */

/* Каналы */
--channel-whatsapp: #25D366;
--channel-telegram: #0088cc;
--channel-avito: #00AAFF;
--channel-vk: #4C75A3;
--channel-max: #8B5CF6;

/* Сообщения */
--message-incoming: #e0f2fe;  /* Светло-синий */
--message-outgoing: #fed7aa;  /* Светло-оранжевый */

/* Фоны */
--bg-primary: #ffffff;
--bg-secondary: #f8fafc;
--bg-sidebar: #1e293b;
```

---

## 6. Real-time функционал

### 6.1 WebSocket подключение

```typescript
// Подключение при авторизации
const socket = io('wss://n8n.n8nsrv.ru/ws/operator', {
  query: {
    operator_id: operatorId,
    tenant_id: tenantId,
    token: sessionToken
  }
});

// События
socket.on('new_message', (data) => {
  // Обновить список диалогов
  // Добавить сообщение в чат
  // Показать уведомление
});

socket.on('show_draft', (data) => {
  // Показать черновик AI
});

socket.on('connection_status', (data) => {
  // Обновить статус каналов
});
```

### 6.2 Browser Notifications

```typescript
// Запрос разрешения
Notification.requestPermission();

// Показ уведомления
new Notification('Новое сообщение', {
  body: 'Иван Петров: Здравствуйте!',
  icon: '/icons/notification.png',
  tag: 'dialog-123',
  data: { dialogId: '123' }
});

// Клик по уведомлению
notification.onclick = () => {
  window.focus();
  navigate(`/dialogs/${notification.data.dialogId}`);
};
```

---

## 7. Голосовой ввод

### 7.1 Web Speech API

```typescript
const recognition = new webkitSpeechRecognition();
recognition.lang = 'ru-RU';
recognition.continuous = true;
recognition.interimResults = true;

recognition.onresult = (event) => {
  const transcript = event.results[event.results.length - 1][0].transcript;
  // Обновить текст в поле ввода
};

recognition.onerror = (event) => {
  if (event.error === 'not-allowed') {
    // Показать инструкцию по разрешению микрофона
  }
};

// Начать запись
recognition.start();

// Остановить запись
recognition.stop();
```

### 7.2 UI индикация

- Кнопка микрофона меняет цвет при записи
- Таймер записи (секунды)
- Визуализация уровня звука (опционально)

---

## 8. Хранение данных

### 8.1 LocalStorage

```typescript
// Сессия
localStorage.setItem('session', JSON.stringify({
  token: string,
  operatorId: string,
  tenantId: string,
  name: string,
  email: string
}));

// Настройки
localStorage.setItem('settings', JSON.stringify({
  notifications: boolean,
  soundEnabled: boolean,
  selectedChannel: string
}));
```

### 8.2 React Query Cache

```typescript
// Кеширование диалогов
const { data: dialogs } = useQuery({
  queryKey: ['dialogs'],
  queryFn: fetchDialogs,
  staleTime: 30000,  // 30 сек
  refetchInterval: 60000  // 1 мин
});

// Кеширование сообщений
const { data: messages } = useQuery({
  queryKey: ['messages', dialogId],
  queryFn: () => fetchMessages(dialogId),
  staleTime: 10000
});
```

---

## 9. Безопасность

### 9.1 Аутентификация
- Session token хранится в localStorage
- Token передаётся в заголовке `Authorization: Bearer {token}`
- При 401 ошибке → автоматический logout

### 9.2 Валидация
- Все входные данные валидируются через Zod
- XSS защита: экранирование HTML в сообщениях
- CSRF: токен в заголовках (если требуется)

### 9.3 HTTPS
- Все API запросы только через HTTPS
- WebSocket через WSS

---

## 10. Структура проекта

```
web-app/
├── public/
│   ├── icons/
│   └── favicon.ico
├── src/
│   ├── api/
│   │   ├── client.ts           # Axios instance
│   │   ├── auth.ts             # Auth endpoints
│   │   ├── dialogs.ts          # Dialogs endpoints
│   │   ├── messages.ts         # Messages endpoints
│   │   └── channels.ts         # Channels endpoints
│   ├── components/
│   │   ├── ui/                 # Базовые UI компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Badge.tsx
│   │   │   └── ...
│   │   ├── dialogs/            # Компоненты диалогов
│   │   │   ├── DialogList.tsx
│   │   │   ├── DialogItem.tsx
│   │   │   └── DialogFilters.tsx
│   │   ├── chat/               # Компоненты чата
│   │   │   ├── ChatHeader.tsx
│   │   │   ├── MessageList.tsx
│   │   │   ├── MessageBubble.tsx
│   │   │   ├── MessageInput.tsx
│   │   │   └── ChannelSelector.tsx
│   │   ├── settings/           # Компоненты настроек
│   │   │   ├── ChannelCard.tsx
│   │   │   └── NotificationSettings.tsx
│   │   └── layout/             # Лейаут компоненты
│   │       ├── Header.tsx
│   │       ├── Sidebar.tsx
│   │       └── MainLayout.tsx
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useDialogs.ts
│   │   ├── useMessages.ts
│   │   ├── useWebSocket.ts
│   │   └── useVoiceInput.ts
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── DialogsPage.tsx
│   │   ├── ChatPage.tsx
│   │   ├── SettingsPage.tsx
│   │   └── channels/
│   │       ├── WhatsAppSetup.tsx
│   │       ├── TelegramSetup.tsx
│   │       └── ...
│   ├── store/
│   │   ├── authStore.ts
│   │   └── uiStore.ts
│   ├── types/
│   │   ├── api.ts
│   │   ├── dialog.ts
│   │   ├── message.ts
│   │   └── channel.ts
│   ├── utils/
│   │   ├── date.ts
│   │   ├── format.ts
│   │   └── validation.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 11. Этапы разработки

### Этап 1: Базовый функционал (MVP)
- [ ] Настройка проекта (Vite + React + TypeScript)
- [ ] Авторизация (login/logout)
- [ ] Список диалогов
- [ ] Экран чата (просмотр сообщений)
- [ ] Отправка текстовых сообщений
- [ ] Базовые настройки

### Этап 2: Каналы
- [ ] Настройка WhatsApp (QR-код)
- [ ] Настройка Telegram (Bot/User)
- [ ] Настройка Avito
- [ ] Настройка MAX

### Этап 3: Real-time
- [ ] WebSocket подключение
- [ ] Real-time обновление сообщений
- [ ] Browser Notifications
- [ ] Статусы подключения каналов

### Этап 4: Дополнительный функционал
- [ ] Голосовой ввод (Web Speech API)
- [ ] AI нормализация текста
- [ ] Omnichannel ответы
- [ ] Фильтрация и поиск диалогов

### Этап 5: Полировка
- [ ] Адаптивный дизайн (mobile)
- [ ] Оптимизация производительности
- [ ] Обработка ошибок
- [ ] Тестирование

---

## 12. API Endpoints (полный список)

### Авторизация
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/v1/auth/login` | Авторизация |
| POST | `/v1/auth/logout` | Выход |

### Диалоги
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/v1/dialogs` | Список диалогов |
| GET | `/v1/messages` | Сообщения диалога |
| POST | `/v1/messages/send` | Отправка сообщения |
| POST | `/v1/normalize` | AI нормализация |

### Каналы
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/v1/channels/register` | Регистрация канала |
| POST | `/v1/register-fcm` | Регистрация push-токена |

### Настройки
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/v1/update-settings` | Обновление настроек |

---

## 13. Требования к окружению разработки

### Локальная разработка
```bash
# Node.js 18+
node --version

# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка production
npm run build
```

### Переменные окружения
```env
VITE_API_URL=https://n8n.n8nsrv.ru/webhook
VITE_WS_URL=wss://n8n.n8nsrv.ru/ws
VITE_WHATSAPP_API_URL=http://155.212.221.189:8769
```

---

## 14. Деплой

### Вариант 1: Static hosting (рекомендуется)
- Netlify
- Vercel
- Cloudflare Pages

### Вариант 2: Собственный сервер
```bash
# Сборка
npm run build

# Результат в папке dist/
# Nginx конфиг для SPA:
location / {
  try_files $uri $uri/ /index.html;
}
```

---

## 15. Контакты

- **Репозиторий:** github.com/eldoleado/web-app
- **API документация:** n8n.n8nsrv.ru/docs
- **Вопросы:** через Issues в репозитории
