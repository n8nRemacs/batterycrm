# 🛠️ n8n Manager - Инструкция по использованию

## ⚙️ Версия n8n: **1.119.2**

⚠️ **КРИТИЧНО:** Перед любыми изменениями читай [N8N_VERSION_INFO.md](N8N_VERSION_INFO.md)

---

## 🚀 Быстрый старт

### 1. Посмотреть все воркеры
```bash
python n8n_manager.py list
```

### 2. Скачать воркер для редактирования
```bash
python n8n_manager.py get <WORKFLOW_ID>
```

Примеры:
```bash
# BAT IN Telegram
python n8n_manager.py get pmDPBdREgE5wf1Cn

# API_Operator_Appeals_List
python n8n_manager.py get 0t7CnDCcHA4MnmTp
```

Файл сохранится в папку `workflows/`

### 3. Редактировать воркер
- Открой файл в `workflows/` в VSCode или любом редакторе
- Измени что нужно
- **ПРОВЕРЬ совместимость с v1.119.2!** (см. N8N_VERSION_INFO.md)
- Сохрани

### 4. Загрузить изменения обратно в n8n
```bash
python n8n_manager.py update <WORKFLOW_ID> "workflows/имя_файла.json"
```

Примеры:
```bash
python n8n_manager.py update pmDPBdREgE5wf1Cn "workflows/BAT IN Telegram_pmDPBdREgE5wf1Cn.json"
```

### 5. Активировать/деактивировать воркер
```bash
# Активировать
python n8n_manager.py activate pmDPBdREgE5wf1Cn

# Деактивировать
python n8n_manager.py deactivate pmDPBdREgE5wf1Cn
```

---

## 📋 Полный пример работы

```bash
# 1. Смотрим список воркеров
python n8n_manager.py list

# 2. Скачиваем нужный воркер
python n8n_manager.py get pmDPBdREgE5wf1Cn

# 3. Редактируем файл workflows/BAT IN Telegram_pmDPBdREgE5wf1Cn.json
#    в VSCode или другом редакторе

# 4. Загружаем изменения обратно
python n8n_manager.py update pmDPBdREgE5wf1Cn "workflows/BAT IN Telegram_pmDPBdREgE5wf1Cn.json"

# 5. Проверяем что воркер работает в n8n UI
```

---

## 🎯 Частые задачи

### Изменить логику в Code ноде
1. Скачай воркер: `python n8n_manager.py get <ID>`
2. Найди нужную ноду в JSON (ищи по `"name": "Название ноды"`)
3. Измени код в поле `"jsCode"`
4. Загрузи обратно: `python n8n_manager.py update <ID> workflows/файл.json`

### Добавить новую ноду
1. Скачай воркер
2. Добавь новый объект в массив `"nodes": []`
3. **ПРОВЕРЬ typeVersion совместимость с 1.119.2!**
4. Добавь связи в `"connections": {}`
5. Загрузи обратно

### Изменить SQL запрос в Postgres ноде
1. Скачай воркер
2. Найди Postgres ноду
3. Измени поле `"query"`
4. Загрузи обратно

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

### ❌ НИКОГДА не делай:
- Не используй ноды/функции из версий > 1.119.2
- Не меняй `typeVersion` на более высокую версию
- Не удаляй обязательные поля (`id`, `name`, `type`, `position`)
- Не работай напрямую в production - сначала тестируй

### ✅ ВСЕГДА делай:
- Проверяй совместимость в [N8N_VERSION_INFO.md](N8N_VERSION_INFO.md)
- Делай бэкап перед изменениями (скачивай воркер)
- Тестируй изменения на копии воркера
- Проверяй что воркер запускается после обновления

---

## 🔍 Полезные команды

### Найти воркер по имени
```bash
python n8n_manager.py list | grep "BAT IN"
```

### Скачать несколько воркеров сразу
```bash
# BAT IN Telegram
python n8n_manager.py get pmDPBdREgE5wf1Cn

# BAT AI Extractor
python n8n_manager.py get 3xsRdfDN9YLaUMvS

# API_Operator_Appeals_List
python n8n_manager.py get 0t7CnDCcHA4MnmTp
```

---

## 📂 Структура файлов

```
BattCRMApp/
├── n8n_manager.py              # Главный скрипт
├── N8N_VERSION_INFO.md         # КРИТИЧНО! Информация о версии
├── README_N8N_MANAGER.md       # Эта инструкция
├── Start.md                    # Общий контекст проекта
└── workflows/                  # Скачанные воркеры
    ├── BAT IN Telegram_pmDPBdREgE5wf1Cn.json
    ├── API_Operator_Appeals_List_0t7CnDCcHA4MnmTp.json
    └── ...
```

---

## 🆘 Решение проблем

### Ошибка "Module not found: requests"
```bash
pip install requests
```

### Ошибка "HTTPError 401: Unauthorized"
API ключ невалиден. Проверь в n8n_manager.py значение `API_KEY`

### Ошибка при обновлении воркера
1. Проверь что JSON валиден (можно через jsonlint.com)
2. Проверь что все обязательные поля на месте
3. Проверь typeVersion совместимость с 1.119.2

### Воркер не запускается после обновления
1. Открой воркер в n8n UI
2. Посмотри ошибки в логах
3. Проверь что все ноды совместимы с v1.119.2
4. Проверь синтаксис в Code нодах

---

## 📞 Где искать помощь

1. **N8N_VERSION_INFO.md** - информация о версии и совместимости
2. **Start.md** - общий контекст проекта
3. **Eldoleado full.md** - архитектура всех воркеров
4. **n8n Docs для v1.119.2** - https://docs.n8n.io/

---

## 🎯 Следующие шаги

После того как освоишь базовую работу:
1. Изучи структуру воркеров в [Eldoleado full.md](Eldoleado%20full.md)
2. Попрактикуйся на неактивных воркерах
3. Начни с простых изменений (текст, SQL запросы)
4. Переходи к более сложным (добавление нод, изменение логики)

**Удачи! 🚀**
