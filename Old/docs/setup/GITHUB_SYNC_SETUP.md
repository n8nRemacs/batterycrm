# Настройка синхронизации с GitHub

**Дата:** 19.11.2025  
**Проект:** BattCRMApp

---

## 📋 Пошаговая инструкция

### Шаг 1: Установка Git (если еще не установлен)

1. Проверьте, установлен ли Git:
   ```powershell
   git --version
   ```

2. Если не установлен, скачайте с [git-scm.com](https://git-scm.com/download/win)
   - Установите с настройками по умолчанию

---

### Шаг 2: Настройка Git (первый раз)

Откройте PowerShell или Git Bash и выполните:

```powershell
# Укажите ваше имя (замените на свое)
git config --global user.name "Ваше Имя"

# Укажите ваш email (замените на свой)
git config --global user.email "your.email@example.com"

# Проверьте настройки
git config --global --list
```

---

### Шаг 3: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name:** `BattCRMApp` (или другое имя)
   - **Description:** "Battery CRM Android Application"
   - **Visibility:** 
     - ✅ **Private** (рекомендуется для коммерческого проекта)
     - или **Public** (если хотите открытый код)
   - ❌ **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license"
5. Нажмите **"Create repository"**

---

### Шаг 4: Инициализация Git в проекте

Откройте PowerShell в папке проекта:

```powershell
# Перейдите в папку проекта
cd C:\Users\User\Dropbox\BattCRMApp

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: Multi-Device Session Management implementation"
```

---

### Шаг 5: Создание .gitignore

Создайте файл `.gitignore` в корне проекта:

```gitignore
# Android
*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Built application files
*.apk
*.ap_
*.aab

# Files for the ART/Dalvik VM
*.dex

# Java class files
*.class

# Generated files
bin/
gen/
out/
release/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/

# Android Studio captures folder
captures/

# IntelliJ
*.iml
.idea/workspace.xml
.idea/tasks.xml
.idea/gradle.xml
.idea/assetWizardSettings.xml
.idea/dictionaries
.idea/libraries
.idea/jarRepositories.xml
.idea/caches
.idea/modules.xml

# Keystore files
*.jks
*.keystore

# External native build folder generated in Android Studio 2.2 and later
.externalNativeBuild
.cxx/

# Google Services (e.g. APIs or Firebase)
google-services.json

# Freeline
freeline.py
freeline/
freeline_project_description.json

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
fastlane/readme.md

# Version control
vcs.xml

# lint
lint/intermediates/
lint/generated/
lint/outputs/
lint/tmp/
lint-results*.xml

# Android Profiling
*.hprof

# Dropbox (если нужно исключить)
# .dropbox

# Временные файлы
*.tmp
*.bak
*.swp
*~
```

---

### Шаг 6: Подключение к GitHub

После создания репозитория на GitHub, скопируйте URL (например: `https://github.com/yourusername/BattCRMApp.git`)

В PowerShell выполните:

```powershell
# Добавьте удаленный репозиторий (замените URL на свой)
git remote add origin https://github.com/yourusername/BattCRMApp.git

# Проверьте подключение
git remote -v

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Права: `repo` (полный доступ к репозиториям)

---

### Шаг 7: Ежедневная работа

#### На компьютере 1 (где работаете):

```powershell
# 1. Проверьте статус изменений
git status

# 2. Добавьте измененные файлы
git add .

# 3. Создайте коммит с описанием
git commit -m "Описание изменений"

# 4. Отправьте на GitHub
git push
```

#### На компьютере 2 (другой компьютер):

```powershell
# 1. Получите последние изменения
git pull

# 2. Работайте с кодом...

# 3. Когда закончите, отправьте обратно
git add .
git commit -m "Описание изменений"
git push
```

---

## 🔄 Типичные команды Git

### Просмотр изменений:
```powershell
git status              # Статус файлов
git diff                # Показать изменения
git log                  # История коммитов
```

### Работа с ветками:
```powershell
git branch               # Список веток
git branch feature-name  # Создать ветку
git checkout feature-name # Переключиться на ветку
git merge feature-name   # Слить ветку в текущую
```

### Синхронизация:
```powershell
git pull                 # Получить изменения с GitHub
git push                 # Отправить изменения на GitHub
git fetch                # Получить информацию об изменениях (без слияния)
```

---

## ⚠️ Важные моменты

### 1. Всегда делайте commit перед переключением компьютеров
```powershell
git add .
git commit -m "Work in progress"
git push
```

### 2. Если забыли сделать push на компьютере 1:
- На компьютере 2 сначала сделайте `git pull`
- Если есть конфликты, Git покажет их
- Разрешите конфликты вручную

### 3. Конфликты при merge:
```powershell
# Если Git показывает конфликты:
# 1. Откройте файлы с конфликтами
# 2. Найдите маркеры <<<<<<< ======= >>>>>>>
# 3. Выберите нужный код
# 4. Сохраните файл
# 5. Выполните:
git add .
git commit -m "Resolve conflicts"
```

---

## 🚀 Быстрый старт (краткая версия)

```powershell
# Первая настройка (один раз)
cd C:\Users\User\Dropbox\BattCRMApp
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/BattCRMApp.git
git push -u origin main

# Ежедневная работа
git add .
git commit -m "Описание"
git push
```

---

## 📝 Рекомендации по коммитам

### Хорошие сообщения коммитов:
```
✅ "Add ErrorInterceptor for 401 handling"
✅ "Update DeviceInfo structure - all fields optional"
✅ "Fix: device_info parsing in LoginActivity"
✅ "Refactor: extract device info methods"
```

### Плохие сообщения:
```
❌ "fix"
❌ "changes"
❌ "update"
❌ "asdf"
```

---

## 🔐 Безопасность

### Что НЕ коммитить в Git:
- ❌ Пароли
- ❌ API ключи
- ❌ `google-services.json` (если содержит секреты)
- ❌ Персональные данные
- ❌ Файлы с токенами

### Что можно коммитить:
- ✅ Код приложения
- ✅ Конфигурационные файлы (без секретов)
- ✅ Документацию
- ✅ `.gitignore`

---

## 🆘 Решение проблем

### Ошибка: "fatal: not a git repository"
```powershell
git init
```

### Ошибка: "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/yourusername/BattCRMApp.git
```

### Ошибка: "failed to push some refs"
```powershell
git pull --rebase origin main
git push
```

### Отменить последний коммит (если еще не push):
```powershell
git reset --soft HEAD~1
```

---

## 📞 Дополнительная помощь

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

**End of Guide**




