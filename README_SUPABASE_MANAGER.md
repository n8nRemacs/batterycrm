# 🗄️ Supabase Manager - Инструкция по использованию

## 📊 Управление схемой базы данных

Этот инструмент позволяет экспортировать схему Supabase БД в файлы для отслеживания изменений через Git.

---

## 🚀 Быстрый старт

### 1. Посмотреть список всех таблиц
```bash
python supabase_manager.py list
```

### 2. Экспортировать схему БД
```bash
python supabase_manager.py export
```

Схема будет экспортирована в папку `database_schema/`:
- `schema_complete.json` - полная схема всех таблиц
- `<table_name>.json` - схема каждой отдельной таблицы

### 3. Добавить в Git
```bash
git add database_schema/
git commit -m "Update database schema"
git push
```

---

## 📋 Таблицы BatteryCRM

Всего 26 таблиц:

### Multi-tenant
- `tenants` - арендаторы (SaaS изоляция)
- `tenant_configs` - конфигурации арендаторов

### Клиенты
- `clients` - клиенты
- `client_merges` - объединение клиентов

### Обращения и сообщения
- `appeals` - обращения
- `messages_history` - история сообщений

### Операторы
- `operators` - операторы
- `operator_actions` - действия операторов
- `operator_appeal_status` - отслеживание обращений операторами
- `operator_devices` - устройства операторов (Android)

### Каналы и маркетинг
- `channels` - каналы коммуникации
- `sales_channels` - каналы продаж
- `lead_sources` - источники лидов (с UTM)

### Справочники устройств и услуг
- `device_types` - типы устройств
- `brands` - бренды
- `models` - модели устройств
- `repair_types` - типы ремонта
- `issue_types` - типы проблем
- `issues` - конкретные проблемы
- `deal_types` - типы сделок
- `services` - услуги и цены
- `masters` - мастера
- `appointments` - записи на прием
- `knowledge_base` - база знаний
- `appeal_meta_config` - конфигурация метаданных обращений
- `appeal_meta_visibility` - правила видимости метаданных

### Firebase
- `fcm_tokens` - токены для push-уведомлений

---

## 🔄 Workflow для отслеживания изменений БД

```bash
# 1. Экспортируй текущую схему
python supabase_manager.py export

# 2. Посмотри что изменилось
git diff database_schema/

# 3. Если изменения есть - закоммить
git add database_schema/
git commit -m "Database schema update: добавлено поле X в таблицу Y"
git push

# 4. Обнови документацию если нужно
# Database_Structure_BatteryCRM_COMPLETE.md
```

---

## 📊 Формат экспортированных данных

### schema_complete.json
```json
{
  "export_date": "2025-11-22T...",
  "database_url": "https://supabase.n8nsrv.ru",
  "total_tables": 26,
  "tables": {
    "clients": { ... },
    "appeals": { ... },
    ...
  }
}
```

### clients.json (пример)
```json
{
  "table_name": "clients",
  "columns": [
    "id", "tenant_id", "phone", "name", "email",
    "telegram_id", "vk_id", "whatsapp_id", ...
  ],
  "row_count": 1
}
```

---

## ⚠️ Важно

### Что отслеживаем в Git
✅ Схема таблиц (structure)
✅ Список колонок
✅ Изменения структуры

### Что НЕ отслеживаем
❌ Данные клиентов (sensitive data)
❌ Содержимое таблиц
❌ Personal information

---

## 🎯 Полезные команды

### Сравнить схему с предыдущей версией
```bash
git diff HEAD~1 database_schema/schema_complete.json
```

### Посмотреть когда менялась схема таблицы clients
```bash
git log -p database_schema/clients.json
```

### Откатить схему к предыдущей версии (для справки)
```bash
git checkout HEAD~1 database_schema/
```

---

## 📞 Где искать помощь

1. **Database_Structure_BatteryCRM_COMPLETE.md** - полная документация по БД
2. **Start.md** - общий контекст проекта
3. **Supabase Dashboard** - https://supabase.n8nsrv.ru

---

## 🔐 Безопасность

- API ключи хранятся в `supabase_manager.py` (НЕ коммитятся в Git если вынести в .env)
- Экспортируется только структура, не данные
- Service Role Key имеет полный доступ - используй осторожно

**Удачи! 🚀**
