# Multi-Device Session Management - Frontend Implementation Summary

**Date:** 19.11.2025  
**Status:** ✅ Implementation Complete  
**Next Step:** Testing after backend is ready

---

## 📋 Обзор изменений

Реализована система управления сессиями для нескольких устройств на Android. Теперь один оператор может быть залогинен максимум на 2 устройствах одновременно (1 mobile + 1 desktop). При новом логине на мобильном устройстве старая мобильная сессия автоматически удаляется на бэкенде, и старое устройство получает 401 ошибку.

---

## 🎯 Требования (из документа бэкендера)

### Правила работы:
- **1 оператор = максимум 2 устройства:** 1 mobile (Android) + 1 desktop (web)
- **Новый мобильный логин** → старая мобильная сессия автоматически удаляется
- **Новый desktop логин** → старая desktop сессия автоматически удаляется
- **Mobile + desktop могут работать одновременно** ✅

### Поведение при замене сессии:
- Старое устройство получает **401 Unauthorized** при следующем API запросе
- Приложение должно обработать 401, очистить сессию и редиректить на логин
- Показать сообщение: "Вы вошли в систему с другого устройства"

---

## ✅ Что было реализовано в этой сессии

### 1. Обновлены структуры данных

#### `LoginRequest` - добавлен опциональный `device_info`
```kotlin
data class LoginRequest(
    val login: String,
    val password: String,
    val device_info: DeviceInfo? = null  // ← НОВОЕ
)
```

**Файл:** `app/src/main/java/com/batterycrm/app/api/ApiService.kt`

---

#### `DeviceInfo` - полностью переработана структура
```kotlin
data class DeviceInfo(
    val device_id: String? = null,        // ← НОВОЕ: Android ID
    val device_model: String? = null,     // ← ИЗМЕНЕНО: было "model"
    val os_version: String? = null,       // ← ИЗМЕНЕНО: было обязательное
    val app_version: String? = null       // ← ИЗМЕНЕНО: было обязательное
)
```

**Изменения:**
- ✅ Переименовано `model` → `device_model` (для совместимости с бэкендом)
- ✅ Все поля сделаны опциональными (`String?`)
- ✅ Добавлено поле `device_id` (Android ID)

**Файл:** `app/src/main/java/com/batterycrm/app/api/ApiService.kt`

---

#### `FCMTokenRegisterRequest` - `device_info` сделан опциональным
```kotlin
data class FCMTokenRegisterRequest(
    val operator_id: String,
    val session_token: String,
    val fcm_token: String,
    val device_info: DeviceInfo? = null  // ← ИЗМЕНЕНО: было обязательное
)
```

**Файл:** `app/src/main/java/com/batterycrm/app/api/ApiService.kt`

---

### 2. Реализована отправка `device_info` при логине

#### Обновлен `LoginActivity`
- Добавлены методы для получения информации об устройстве:
  - `getDeviceInfo()` - создает объект `DeviceInfo`
  - `getAndroidId()` - получает Android ID
  - `getDeviceModel()` - получает модель устройства (Manufacturer + Model)
  - `getOsVersion()` - получает версию Android
  - `getAppVersion()` - получает версию приложения

- При логине создается и отправляется `device_info`:
```kotlin
val deviceInfo = getDeviceInfo()
val request = LoginRequest(login = email, password = password, device_info = deviceInfo)
```

**Файл:** `app/src/main/java/com/batterycrm/app/LoginActivity.kt`

**Примечание:** Все методы безопасно обрабатывают ошибки и возвращают `null` при неудаче.

---

### 3. Обновлен `FCMRepository`

#### Методы получения device info
- Используются те же методы, что и в `LoginActivity`
- `device_info` отправляется при регистрации FCM токена
- Все поля опциональные, безопасная обработка ошибок

**Файл:** `app/src/main/java/com/batterycrm/app/fcm/FCMRepository.kt`

---

### 4. Реализована глобальная обработка 401 ошибок

#### Создан `ErrorInterceptor`
Новый класс для автоматической обработки 401 ошибок во всех API запросах.

**Функциональность:**
- ✅ Перехватывает все HTTP ответы
- ✅ Проверяет код ответа 401
- ✅ Исключает обработку 401 для `/android/auth/login` и `/android/logout` (чтобы избежать рекурсии)
- ✅ Очищает сессию через `SessionManager`
- ✅ Показывает Toast сообщение: "Вы вошли в систему с другого устройства"
- ✅ Редиректит на `LoginActivity` с флагами `FLAG_ACTIVITY_NEW_TASK | FLAG_ACTIVITY_CLEAR_TASK`
- ✅ Защита от множественных вызовов (флаг `isHandlingUnauthorized`)

**Файл:** `app/src/main/java/com/batterycrm/app/api/ErrorInterceptor.kt`

**Интеграция:**
- Добавлен в `RetrofitClient` в цепочку interceptors
- Порядок: `AuthInterceptor` → `ErrorInterceptor` → `LoggingInterceptor`

**Файл:** `app/src/main/java/com/batterycrm/app/api/RetrofitClient.kt`

---

## 🔄 Принцип работы системы

### Сценарий 1: Нормальный логин

```
1. Пользователь вводит credentials в LoginActivity
2. Приложение создает DeviceInfo (Android ID, модель, ОС, версия приложения)
3. Отправляется LoginRequest с device_info
4. Бэкенд:
   - Проверяет credentials
   - Удаляет старую мобильную сессию (если есть)
   - Создает новую запись в operator_devices
   - Сохраняет device_info в JSONB поле
5. Возвращает session_token
6. Приложение сохраняет session_token в SessionManager
7. Регистрирует FCM токен (также с device_info)
```

---

### Сценарий 2: Логин на втором устройстве

```
Device A (старое):
1. Пользователь уже залогинен, работает в приложении
2. Пользователь логинится на Device B

Device B (новое):
1. Логин успешен
2. Бэкенд удалил сессию Device A из operator_devices
3. Device B получает новый session_token
4. Device B работает нормально

Device A (старое):
1. Пользователь делает любой API запрос (например, загрузка списка обращений)
2. Бэкенд проверяет session_token в operator_devices
3. Сессия не найдена → возвращает 401 Unauthorized
4. ErrorInterceptor перехватывает 401:
   - Очищает сессию через SessionManager
   - Показывает Toast: "Вы вошли в систему с другого устройства"
   - Редиректит на LoginActivity
5. Пользователь видит экран логина
```

---

### Сценарий 3: Регистрация FCM токена

```
1. После успешного логина вызывается registerFCMToken()
2. Получается FCM токен от Firebase
3. Создается DeviceInfo (если еще не создан)
4. Отправляется FCMTokenRegisterRequest с:
   - operator_id
   - session_token
   - fcm_token
   - device_info (опционально)
5. Бэкенд обновляет запись в operator_devices:
   - Обновляет fcm_token
   - Обновляет device_info (если приходит)
   - Обновляет last_active_at
```

---

## 📝 Договоренности с бэкендером

### Структуры данных (финальные)

#### `LoginRequest`
```kotlin
data class LoginRequest(
    val login: String,                    // email или username
    val password: String,
    val device_info: DeviceInfo? = null  // Опционально
)
```

#### `DeviceInfo`
```kotlin
data class DeviceInfo(
    val device_id: String? = null,       // Android ID (опционально)
    val device_model: String? = null,    // Модель устройства (опционально)
    val os_version: String? = null,       // Версия ОС (опционально)
    val app_version: String? = null      // Версия приложения (опционально)
)
```

#### `FCMTokenRegisterRequest`
```kotlin
data class FCMTokenRegisterRequest(
    val operator_id: String,
    val session_token: String,
    val fcm_token: String,
    val device_info: DeviceInfo? = null  // Опционально
)
```

---

### Endpoints (не меняются)

- `POST /webhook/android/auth/login` - логин
- `POST /webhook/android/logout` - выход
- `POST /webhook/android-register-fcm` - регистрация FCM токена

---

### Ответы на вопросы бэкендера

#### 1. Добавить парсинг device_info в API_Android_Auth?
**Ответ:** ✅ ДА, нужно добавить парсинг `device_info` в воркфлоу бэкенда

#### 2. Реальный путь API_Operator_Appeals_List?
**Ответ:** `/webhook/dbc87d6b-d3a8-4613-a2d8-b591d5533210/api/operator/appeals/list`

#### 3. Api_Voice_Uploader требует обновления?
**Ответ:** ❌ НЕТ, не требует (не использует session_token для аутентификации)

---

### Что делает бэкендер

1. **Создает таблицу `operator_devices`** с полями:
   - `id`, `operator_id`, `tenant_id`
   - `device_type` ('mobile' или 'desktop')
   - `session_token` (UNIQUE)
   - `fcm_token`
   - `device_id`, `device_info` (JSONB)
   - `last_active_at`, `created_at`
   - Constraint: `UNIQUE(operator_id, device_type, tenant_id)`

2. **Обновляет API_Android_Auth:**
   - Удаляет старую мобильную сессию перед созданием новой
   - Сохраняет новую сессию в `operator_devices`
   - Сохраняет `device_info` (если приходит)

3. **Обновляет API_Android_Register_FCM:**
   - Ищет устройство в `operator_devices` по `session_token`
   - Обновляет `fcm_token` и `device_info`

4. **Обновляет API_Android_Logout:**
   - Удаляет запись из `operator_devices`

5. **Обновляет все Operator API:**
   - Проверяют `session_token` в `operator_devices` вместо `operators`
   - Проверяют `device_type = 'mobile'`
   - Возвращают 401, если сессия не найдена

**Подробный чеклист для бэкендера:** `Backend Implementation Checklist - Multi-Device Session Management.md`

---

## 🧪 Тестирование

### Что можно тестировать сейчас:
- ✅ Код компилируется без ошибок
- ✅ `device_info` отправляется при логине (можно проверить в логах)
- ✅ `device_info` отправляется при регистрации FCM
- ✅ Структуры данных соответствуют требованиям

### Что нужно тестировать после готовности бэкенда:

#### Тест 1: Логин на двух устройствах
1. Device A: логин → должен работать
2. Device B: логин (те же credentials) → должен работать
3. Device A: следующий API запрос → должен получить 401
4. Device A: должен показать сообщение и редирект на логин

#### Тест 2: Проверка device_info
1. Проверить в базе данных, что `device_info` сохраняется при логине
2. Проверить, что `device_info` обновляется при регистрации FCM

#### Тест 3: Все Operator API
1. После логина вызвать каждый Operator API
2. Все должны работать с новым `session_token`
3. После logout все должны вернуть 401

---

## 📁 Измененные файлы

### Новые файлы:
1. `app/src/main/java/com/batterycrm/app/api/ErrorInterceptor.kt` - обработка 401 ошибок

### Измененные файлы:
1. `app/src/main/java/com/batterycrm/app/api/ApiService.kt`
   - Обновлен `LoginRequest` (добавлен `device_info`)
   - Обновлен `DeviceInfo` (все поля опциональные, `device_model` вместо `model`, добавлен `device_id`)
   - Обновлен `FCMTokenRegisterRequest` (`device_info` опциональный)

2. `app/src/main/java/com/batterycrm/app/LoginActivity.kt`
   - Добавлены методы получения device info
   - Отправка `device_info` при логине

3. `app/src/main/java/com/batterycrm/app/fcm/FCMRepository.kt`
   - Обновлены методы получения device info
   - Используется новая структура `DeviceInfo`

4. `app/src/main/java/com/batterycrm/app/api/RetrofitClient.kt`
   - Добавлен `ErrorInterceptor` в цепочку interceptors

---

## 🔍 Детали реализации

### ErrorInterceptor - как это работает

```kotlin
class ErrorInterceptor(private val context: Context) : Interceptor {
    // Перехватывает все HTTP ответы
    override fun intercept(chain: Interceptor.Chain): Response {
        val response = chain.proceed(request)
        
        // Если получили 401
        if (response.code == 401) {
            // Исключаем логин и logout (чтобы не было рекурсии)
            if (!url.contains("/android/auth/login") && !url.contains("/android/logout")) {
                handleUnauthorized()
            }
        }
        return response
    }
    
    private fun handleUnauthorized() {
        // 1. Очищаем сессию
        sessionManager.clearSession()
        
        // 2. Показываем сообщение (в главном потоке)
        handler.post {
            Toast.makeText(context, "Вы вошли в систему с другого устройства", ...).show()
        }
        
        // 3. Редиректим на логин
        handler.post {
            val intent = Intent(context, LoginActivity::class.java).apply {
                flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK
            }
            context.startActivity(intent)
        }
    }
}
```

**Важно:**
- Используется `Handler` для работы в главном потоке (UI операции)
- Флаг `isHandlingUnauthorized` предотвращает множественные вызовы
- Исключаются `/android/auth/login` и `/android/logout` из обработки

---

### DeviceInfo - получение данных устройства

```kotlin
private fun getDeviceInfo(): DeviceInfo {
    return DeviceInfo(
        device_id = getAndroidId(),           // Settings.Secure.ANDROID_ID
        device_model = getDeviceModel(),      // "Samsung Galaxy S21"
        os_version = getOsVersion(),         // "Android 13"
        app_version = getAppVersion()         // BuildConfig.VERSION_NAME
    )
}
```

**Безопасность:**
- Все методы обернуты в try-catch
- Возвращают `null` при ошибке
- Не крашат приложение при проблемах с получением данных

---

## ⚠️ Важные замечания

### 1. Порядок interceptors важен
```kotlin
.addInterceptor(AuthInterceptor(sessionManager))  // 1. Добавляет токен
.addInterceptor(ErrorInterceptor(context))        // 2. Обрабатывает ошибки
.addInterceptor(loggingInterceptor)              // 3. Логирует запросы
```

### 2. device_info опциональный
- Если не удалось получить данные устройства → отправляется `null`
- Бэкенд должен корректно обрабатывать отсутствие `device_info`
- Это не критично для работы приложения

### 3. 401 обработка глобальная
- Работает для всех API запросов автоматически
- Не нужно добавлять обработку 401 в каждый `Callback`
- Исключения: `/android/auth/login` и `/android/logout`

### 4. Совместимость
- Все изменения обратно совместимы
- Старые версии бэкенда будут работать (просто проигнорируют `device_info`)
- После обновления бэкенда система начнет работать полностью

---

## 📞 Следующие шаги

### Для фронтендера:
1. ✅ Код готов, компилируется
2. ⏳ Дождаться готовности бэкенда
3. 🧪 Протестировать сценарии после готовности бэкенда
4. 🐛 Исправить баги, если найдутся

### Для бэкендера:
1. Создать таблицу `operator_devices`
2. Обновить все API согласно чеклисту
3. Уведомить фронтендера: "✅ Ready for testing"

---

## 📚 Дополнительные документы

1. **Backend Implementation Checklist** - детальный чеклист для бэкендера
   - `Backend Implementation Checklist - Multi-Device Session Management.md`

2. **Исходный документ от бэкендера** - требования и спецификация
   - `Multi-Device Session Management - Android Implementation`

---

## ✅ Чеклист готовности

- [x] Структуры данных обновлены
- [x] `device_info` отправляется при логине
- [x] `device_info` отправляется при регистрации FCM
- [x] 401 interceptor реализован и интегрирован
- [x] Код компилируется без ошибок
- [x] Все импорты добавлены
- [x] Обработка ошибок реализована
- [ ] Тестирование с бэкендом (после готовности)

---

**End of Document**





